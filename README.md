# Pet Coin Breaker

Pet Coin Breaker の最新版配信用リポジトリです。

Windows ランチャーは起動時に `manifest.json` を確認し、新しいバージョンがある場合だけ `game/part01.b64` などの圧縮データを取得してゲームを更新します。

- セーブデータはPC側に保存
- 更新失敗時は前回のゲームで起動
- 現在の配信バージョン: 1
